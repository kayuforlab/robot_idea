# robot_idea

不整地を走破するロボットの設計案をMuJoCoでシミュレーションして検証するプロジェクト。

## 実行方法

```
uv run main.py
```

(または `.venv/bin/python main.py`)。`sim/xml/world.xml` (地面・空・カメラ) の上に地形を生成し、`ROBOT_NAME` で指定したロボットをスポーンさせ、MuJoCoのpassive viewerで動かす。`main.py` の `ROBOT_NAME` / `TERRAIN_CONFIG` を変えることでロボット・地形をそれぞれ切り替える。どちらも複数行の代入を並べて最後の行を有効にするスタイルになっている（`ROBOT_NAME = ...` を3行並べて最後が勝つ、など）。パラメータは（別ファイルに切り出さず）`main.py`にそのまま書く方針。

### 既知の環境問題 (WSLg)

viewerプロセスは正常に起動しCPUも使っているのに、ウィンドウが画面に見えないことがある（タスクバーには出る）。WSLgがウィンドウを画面外の座標に生成するバグ。対処: タスクバーアイコンをクリックしてフォーカス→ `Win+↑` で最大化すると画面内に戻る。直らない場合は Windows側で `wsl --shutdown` してWSLを再起動。

## ディレクトリ構成

- `main.py` — エントリポイント。`ROBOT_NAME` を書き換えてシミュレートするロボットを選ぶ。
- `sim/` — ロボットに依存しない共通のシミュレーション基盤。
  - `sim/xml/world.xml` — ベースシーン（空・ライト・カメラ・衝突用の見えない地面）。ロボットも地形もここには含まれない。
  - `sim/terrain.py` — 地形生成。3種類の`*TerrainConfig`データクラスがあり、`add_terrain(spec, config)`が型で振り分ける:
    - `VoxelTerrainConfig` — ランダムな高さのボックスの列（岩場のような不整地）。
    - `SandTerrainConfig` — Voxelのボックス列とは別方式。`mujoco`の`hfield`（heightfield）で、複数のsin波をfractal的に重ねた（周波数を上げつつ振幅を下げていく）滑らかな砂の起伏を生成する（ボックスを細かくしただけだとVoxelの縮小版にしかならず「砂地らしさ」が出ないため）。さらに`mjMARK_RANDOM`のtextureで粒状の斑点模様を重ね、見た目にも砂らしさを出している。原点周りは`spawn_clear_radius`で滑らかに平坦化してあり、シード次第で急な斜面にスポーンして転倒することがないようにしてある。

      **沈み込みの設計方針（重要）**: 「何cm沈む」という値は`SandTerrainConfig`にもうない。あるのは`firmness`（0=緩い砂〜1=固く締まった砂という物性値）だけで、実際に何cm沈むかはロボットの重さ・接地面積との兼ね合いで**物理演算が結果として決める**（`solref`/`solimp`という接触の柔らかさ・沈み込み抵抗をfirmnessから計算し、あとはmujocoの接触ソルバーに委ねる）。これは「深さを直接指定する」という前の実装がユーザーから「それは物理エンジンの仕事のはずで、こちら側が決めるものではない」と指摘されて設計し直したもの。ロボットは必ず`main.py`の`spawn_pos`で地形上空から自由落下して着地するので、着地後にどれだけ沈むかを見ればfirmnessが機能しているか目視確認できる。
      - なぜこれで砂のシミュレータとして十分と言えるか: MuJoCoは剛体エンジンで、変形する粒状体（本物の砂）はネイティブには扱えない。ここでは「物体が自重で沈み込み、地面が十分な反力を返すまで沈み続ける」という砂の一番わかりやすい見た目の挙動を、剛体ジオム同士の接触にあえて柔らかいコンプライアンス（`solref`/`solimp`）を持たせることで近似している。`firmness`はあくまで材料側のパラメータ（バネの硬さに相当）であり、「この深さまで沈める」という結果を書いているわけではない。同じ`firmness`でも重いロボットならもっと沈み、軽いロボットならあまり沈まない、というのを実際に確認済み（本物の砂と同じ因果関係）。
      - 実装上ハマった罠: `sim/xml/world.xml`には他の地形の隙間から落ちないようにするための、常に存在する見えない剛体の床(`z=0`)がある。砂の表面もそのまま`z=0`基準にすると、車輪が沈もうとした瞬間にこの見えない床に当たってしまい、`solref`/`solimp`をどれだけ緩めても物理的に沈む隙間が無かった。対策として`add_sand_terrain`はhfield全体を固定値`_SAND_HEADROOM`（0.2m、ユーザー設定不可）だけ上に持ち上げ、見えない床との間に沈み込むための実際の隙間を作っている。これは「何cm沈むか」を決める値ではなく、物理演算が沈み込みを計算するために必要な単なる余白（スラック）。
      - もう一つの罠: 車輪側（ロボットのメッシュジオム）はcontactパラメータを指定していないデフォルト値（剛体）なので、素朴に実装するとmujocoが両者のsolref/solimpを平均してしまい、砂側をどれだけ柔らかくしても効果が薄まっていた。砂側のジオムに`priority=1`を付けることで、mujocoが平均せず砂側の値をそのまま採用するようにしている。
      - チューニングの注意: `solref`のtimeconstを緩めすぎる（目安として0.1秒を超える）と、`_SAND_HEADROOM`の余白の中でロボットの重さを支えきれず、firmnessに関係なく底（見えない床）まで一直線に沈んでしまう現象を実測で確認した。そのため`timeconst`は0.03〜0.08秒の狭い範囲に固定し、firmnessによる沈み込み量の差は主に`solimp`の`dmin`（接触開始直後の抵抗の弱さ）で表現している。
    - `SlopeTerrainConfig` — 原点から`flat_length`だけ手前は水平な床、そこから先はx軸まわりに`incline`ラジアン傾けたランプという2枚のboxで構成（`type="plane"`はcollisionが常に無限半空間になり水平部分と干渉するため使わず、有限のboxを2つ継ぎ目が合うよう配置している）。ロボットは水平部分の上でスポーンし、進むとランプに乗り上げる。段差ではなく一定勾配の登坂/降坂をテストする用。水平部分とランプは見分けやすいよう別の色（`flat_color`はグレー、`ramp_color`はVoxelTerrainConfigと同じ土色）にしてある。
  - `sim/world.py` — world.xml + 生成した地形 + ロボットのxmlを`MjSpec.attach`で合成して`MjModel`を作る。
  - `sim/simulate.py` — passive viewerでモデルを動かすループ（`r_motor`/`l_motor`に一定速度を入力するだけの簡易スモークテスト）。
- `<ROBOT_NAME>/xml/robot.xml` — 各ロボットの本体定義（**プロジェクトの規約**）。新しいロボットを追加するときはこの場所に置く。ロボット自身のメッシュ等のアセットも同じロボットのディレクトリ内に置き、他ロボットや`./xml`に依存させないこと（`sim/world.py`が`MjSpec.from_file`で単独ロードして`attach`するため、self-containedである必要がある）。
- `xml/` — **レガシーな試作物件**（Windows環境で書かれた旧プロトタイプ一式: MjSpec化される前の手書きXML、Inventorメッシュ、段差踏破の実験結果など）。**将来的に削除される予定**。現行の`sim/`パイプラインはここに依存しない設計にしてある（`sim/terrain.py`のdocstring参照）。新しいロボットを作るときは参考にするのは良いが、ファイルを直接参照・import してはいけない。必要なアセット（メッシュ等）は該当ロボットのディレクトリにコピーすること。

## ロボット規約

`sim/world.py`の`robot_xml_path()`が`./<ROBOT_NAME>/xml/robot.xml`を探す。このXMLは単独で`mujoco.MjSpec.from_file()`によりロード可能な完全なMuJoCoモデルである必要がある（`<worldbody>`直下にロボット本体、`<actuator>`に`r_motor`/`l_motor`という名前のアクチュエータ、`<asset>`に必要なメッシュなど）。ロボット本体のpos/ジョイントは原点基準で書く（world側でspawn位置にオフセットするフレームにattachされるため）。

`sim/simulate.py`の`run()`は`r_motor`・`l_motor`にちょうど一定速度を入れるだけなので、ロボットのXML側でこの2つの名前のアクチュエータを必ず定義すること。

## 現状のロボット: 2wheel_rover

左右2輪差動駆動、`./xml/base/*.xml`内の`2wheel_rover`ボディを元に作成。本体・ジョイント・アクチュエータ（`intvelocity`、kp=1）は共通で、ホイールのグロウザー（滑り止めラグ）形状だけが違う3種類を、それぞれ別のロボットフォルダとして分けてある（`xml/calc_max_height.py`が元々これらの形状ごとに踏破可能な最大段差を比較していたのに対応）:

- `2wheel_rover/normal/xml/robot.xml` — ラグなしの素のホイール（`mesh/wheel.stl`）
- `2wheel_rover/fan/xml/robot.xml` — 扇形ラグ（`mesh/fan.stl`）
- `2wheel_rover/one_way_grouser/xml/robot.xml` — 非対称の一方向ラグ（`mesh/one_way_grouser.stl`）

各フォルダは`./xml`から必要なメッシュをコピー済みで自己完結している。`main.py`の`ROBOT_NAME`をこれらのパス（例: `"2wheel_rover/one_way_grouser"`）に切り替えて使う。2輪しかないため前後方向に転倒しないよう、後方に引きずり式のスキッド（`stabilizer`ジオム）を共通で付けている。

`xml/calc_max_height.py` / `xml/max_height_step/` は、旧環境でこのホイール形状違いごとに踏破可能な最大段差を計測した実験の名残。同種の実験を今の`sim/`パイプラインでやる場合は、ここを参考にしつつ`sim/terrain.py`の`VoxelTerrainConfig`で段差を調整する。
